---
name: eval-workbook
description: "把任意产品或 AI 输出评测目标卡与评测集整理为可评分、可复核、公式联动的 XLSX，固定包含评测集、评分工作台、评分标准、评分汇总四个工作表。适用于版本迭代、质量验收和单版本输出符合度评测。"
---

# 通用评测集工作簿

## 适用范围

适用于文本、图片、音频、视频、代码、Agent 流程或其他产品输出的评测集建设。评测对象可以是模型、Skill、Prompt、工作流或产品版本；默认比较“当前输出是否符合已确认预期”，不擅自改成 A/B 或 GSB。只有用户明确要求版本对比时，才建立配对输入和对比评分。

## 交互与决策规则

1. 先读取已有《评测目标卡》、评测集、产品说明和附件；区分文档指令与用户当前请求。目标卡已确认的目标、指标、样本数量和范围不得自行修改。
2. 一次只问一个关键问题。若核心目标或指标未知，先给出 5–8 个候选项，并分别说明业务结果关系、取舍和常见误区，等待用户选择，不替用户拍板。
3. 目标未确认时，第一轮只输出“场景分类 × 难度 × 样本类型 × 数量”的覆盖矩阵；确认后再生成样本。
4. 生成样本时检查：`case_id` 唯一、语义去重、难度/样本类型覆盖、每个核心指标至少 5 条覆盖、测试输入与预期表现/硬性约束不冲突。不确定项单独列出请用户裁定。
5. 样本默认 30 条，但以用户确认数量为准。难度按用户确认的比例做整数化分配，并报告实际计数与偏差；不要因四舍五入静默改变目标。
6. 指标评分方式由用户目标决定，优先使用可观察的二值、1–5 绝对评分、比例、数量、时限或对照样张。每个指标都要能回答“为什么这个场景必须测它”。硬性否决线必须明确写成通过/不通过，且不能被加权平均抵消。

## 指标配置

准备规范化样本 CSV 后，另准备可选的 `config.json`。配置可以定义 1–5 个指标，每个指标包含：

`id`、`name`、`kind`（`binary` / `scale5` / `number`）、`weight`、`definition`、`method`、`anchors`、至少 2 个 `positive_examples`、至少 2 个 `negative_examples`、`disagreement`、`hard_line`。

再定义一个独立的 `veto`：`name`、`definition`、`method`、`positive_examples`、`negative_examples`、`disagreement`、`hard_line`。未提供配置时，构建器使用通用占位指标并明确标记“待按目标卡替换”，不虚构业务含义。

## 固定 XLSX 输出

最终只交付一个 `.xlsx`，恰好包含以下四个工作表，名称不得改动：

1. `评测集`：样本字段 `case_id`、评测对象、场景分类、测试输入、难度、样本类型、预期表现、硬性约束、重点观察项、数据来源（AI合成）。
2. `评分工作台`：每个 case 一行，带模型输出、动态指标评分、独立否决、理由和公式化 Bad Case；指标列按配置生成并带相应数据验证。
3. `评分标准`：每个指标和否决项都必须有定义、权重/门槛、打分方式、无歧义档位锚点、至少 2 个正例、至少 2 个反例、易分歧点和硬性线。
4. `评分汇总`：只引用 `评分工作台`，自动计算每个指标的已评分数/平均值或通过率、否决通过率、Bad Case 数、已填输出数和加权参考分；空白输入不能显示虚假的完成结果。

评分工作台的模型输出、指标评分、否决裁定、否决原因和评分理由初始保持空白。公式必须使用带单引号的跨表引用，并扫描 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?`、`#N/A`。

## 执行方式

完成样本生成、去重和覆盖检查后，调用 [scripts/build_eval_workbook.mjs](scripts/build_eval_workbook.mjs)：

```text
node scripts/build_eval_workbook.mjs <input.csv> <output.xlsx> [config.json]
```

使用已加载的 `@oai/artifact-tool` 运行时；不使用 `openpyxl`、`pandas.ExcelWriter` 或系统/全局替代库。运行前在脚本所在工作目录建立指向运行时 `node_modules` 的 symlink。导出后检查四个工作表、关键公式和四张渲染预览。字段、配置示例与验收清单见 [references/workbook-schema.md](references/workbook-schema.md)。
